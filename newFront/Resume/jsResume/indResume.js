let canv = document.querySelector(".canva"),
  //Ниже будет переменная для хранения контекста 2D-рендеринга — фактического инструмента, который мы можем использовать для рисования на холсте.
  view = canvas.getContext("2d");
